[package]
name = "cf-compress-engine"
version = "0.1.0"
edition = "2021"

[dependencies]
image = { version = "0.25", default-features = false, features = ["jpeg"] }
rayon = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
